# TAC-to-Solidity training learnings

This file is the persistent research log for TAC-to-Solidity training runs. It records evidence-backed learnings, hypotheses, invalidated assumptions, evaluator issues, and next experiments from the five-iteration research loop started after the 300-row Qwen QLoRA results.

## Current verdict

**Model quality issue; raw all-row scale-up is diagnostic but not an accepted improvement.** Overfit sanity checks can memorize exactly up to at least 30 seeded examples, which validates prompt/label wiring, adapter save/load, checkpoint evaluation, and multi-GPU execution at small scale. The largest measured model-side gain still comes from exposing locally resolved selector-signature provenance in prompt metadata: on a 30-row non-overlap eval, the old 100-row adapter improved from 0/30 to 19/30 strict signature matches and from 0.3022 to 0.6788 semantic similarity. The 2026-07-02 raw full-source `run_train_qwen_qlora_500.sh` run completed on all 31,577 rows for 1 epoch, but its 1,890-row built-in heldout eval underperformed the earlier default body-balanced full run on F1/semantic/bytecode, and its fixed gate-suite pass is contaminated because every fixed gate dataset overlapped the raw train/val/test split. The eval-clean body-balanced run still has no final model/results, so clean full-run acceptance remains open. Remaining failures are still model/data quality, dominated by calls, state writes, selectors, guards, returns, events, hallucinated facts, and long-output scaffold runaways.

## Iteration log

| Iteration | Artifact(s) | Configuration | Key result | Learning |
| --- | --- | --- | --- | --- |
| 1. 300-row train-row eval | `results/eval_1782603004.json`, model `models/qwen2_5_coder_7b_qlora_500_20260627-160610/` | Existing 300-row run evaluated on first 17 train rows | Semantic similarity ~0.294, normalized edit distance ~0.733, replication F1 micro ~0.298, bytecode score ~0.173; old signature metric reported 1.0 but was misleading | The 300-row model was not just failing heldout generalization; it was weak even on training examples |
| 2. Corrected 10-row overfit sanity | `results/eval_1782604331.json`, model `models/qwen2_5_coder_7b_qlora_overfit_sanity_loop_iter2_seeded10_no_train_eval_gpu4/` | 10 seeded rows, identical train/val/test, 30 epochs, 4 GPUs, global batch 4, LoRA rank 32/alpha 64/dropout 0, `TRAIN_EVAL_STRATEGY=no` | 10/10 exact matches, semantic similarity ~1.0, edit distance 0, replication F1 1.0, hallucination 0 | Basic training/eval wiring works when early stopping is removed and the run is allowed to finish |
| 3. Evaluator metric fix | `src/training_pipeline.py`, `tests/test_evaluation_quality_metrics.py` | Replaced `function_signature_match` keyword-presence check with strict normalized ABI signature comparison | Recomputed old 300-row heldout/train evals: strict signature match 0/17 for both `results/eval_1782602299.json` and `results/eval_1782603004.json`; overfit remains 10/10 | Prior `function_signature_match_mean=1.0` was invalid for wrong function names; use the strict metric going forward |
| 4. Bounded 100-row update-heavy run | `results/eval_1782605340.json`, `results/eval_1782605444.json`, model `models/qwen2_5_coder_7b_qlora_500_loop_iter4_sample100_updates/` | 100 sampled rows -> 83 train / 11 val / 6 test, 10 epochs, 4 GPUs, global batch 4, LoRA rank 32/alpha 64/dropout 0, `TRAIN_EVAL_STRATEGY=no`, `eval_max_new_tokens=512` | Heldout test: 0/6 strict signatures, semantic ~0.289, replication F1 micro ~0.294. Train-first-30: 21/30 exact, 22/30 strict signatures, semantic ~0.812, edit distance ~0.214, replication F1 micro ~0.725 | More optimizer updates and higher LoRA capacity improve memorization, but heldout behavior is still dominated by unrelated/memorized snippets |
| 5. 30-row overfit confirmation | `results/eval_1782607225.json`, model `models/qwen2_5_coder_7b_qlora_overfit_sanity_loop_iter5_overfit30_seeded/` | 30 seeded rows, identical train/val/test, 30 epochs, 4 GPUs, global batch 4, LoRA rank 32/alpha 64/dropout 0, `TRAIN_EVAL_STRATEGY=no`, `eval_max_new_tokens=512` | 30/30 exact matches, 30/30 strict signatures, semantic similarity ~1.0, edit distance 0, replication F1 micro 1.0, train loss ~0.213 after 30 epochs | The pipeline can memorize a larger same-row sample; remaining failures are more likely data/generalization/conditioning issues than core train/eval wiring |
| 6. Selector recoverability audit | `data/qwen_qlora_500_loop_iter4_sample100_updates/splits/*.jsonl`, local `data/contracts.db` via `SelectorResolver(use_remote=False)` | Checked selector resolution for the exact iteration-4 100-row train/val/test split without model inference | Local high-confidence selector resolution recovered 83/83 train, 11/11 val, and 6/6 test signatures exactly | Function identity was recoverable from existing selector provenance, but the model prompt only exposed raw selector hex |
| 7. Selector-signature prompt ablation on heldout-6 | `results/eval_1782608360.json` vs `results/eval_1782605340.json`, old model `models/qwen2_5_coder_7b_qlora_500_loop_iter4_sample100_updates/` | Same 6 heldout rows, same old adapter, same greedy decoding and `eval_max_new_tokens=512`; only prompt metadata changed to include local `selector_signature=...` | Semantic similarity improved 0.2889 -> 0.7923, edit distance 0.7100 -> 0.4561, replication F1 micro 0.2936 -> 0.6226, strict signatures 0/6 -> 4/6, selector mismatches 47 -> 7 | Selector signature metadata fixes a major prompt/eval mismatch even without retraining, but 6 examples remain smoke-only |
| 8. Broad 30-row selector-off/on ablation | `results/eval_1782608489.json` and `results/eval_1782608594.json`, eval data `data/loop_iter10_selector_prompt_broad_eval/broad30_excluding_iter4.jsonl` | 30 rows sampled from `data/hf_training_dataset.jsonl` excluding all iteration-4 100-row body hashes; old adapter evaluated with selector metadata disabled vs enabled | Selector-on improved semantic 0.3022 -> 0.6788, replication F1 micro 0.2144 -> 0.5524, bytecode semantic 0.1241 -> 0.2244, strict signatures 0/30 -> 19/30; prompt truncation stayed 0 | The selector prompt improvement is trustworthy beyond the 6-row smoke split; remaining errors are not due to prompt truncation |
| 9. Selector-metadata 100-row retrain | `results/eval_1782609484.json`, model `models/qwen2_5_coder_7b_qlora_500_loop_iter12_selector_metadata_100/`, manifest `models/qwen2_5_coder_7b_qlora_500_loop_iter12_selector_metadata_100/final_model/training_input_manifest.json` | Same 100-row sample/splits as iteration 4, 10 epochs, 4 GPUs, global batch 4, LR 2e-4, LoRA rank 32/alpha 64/dropout 0, selector metadata enabled, `TRAIN_EVAL_STRATEGY=no`, `eval_max_new_tokens=512` | Heldout-6 got semantic 0.7746, replication F1 micro 0.5378, strict signatures 4/6, train loss 0.3443 after 210 steps | Retraining with selector metadata is viable but did not beat the old-adapter prompt-only ablation on the tiny heldout-6 split |
| 10. Retrain train/broad checks | `results/eval_1782609635.json`, `results/eval_1782609733.json` | Evaluated iteration-9 model on first 30 train rows and the same broad 30 non-overlap slice | Train-first-30: semantic 0.9833, edit distance 0.0383, replication F1 micro 0.9807, strict signatures 29/30, exact 27/30. Broad-30: semantic 0.6933, replication F1 micro 0.5954, bytecode semantic 0.2374, strict signatures 24/30 | Selector-metadata retraining improves memorization and broad-slice structured metrics over the old adapter, but behavior remains below target and deployability is still 0 |
| 11. Context-limited compiler diagnostic fix | `src/training_pipeline.py`, `tests/test_evaluation_quality_metrics.py`, recomputed existing outputs from `results/eval_1782607225.json`, new broad eval `results/eval_1782615573.json` | Treat solc errors caused only by missing contract-level context (`onlyOwner`, storage vars, library members) as syntax-valid function fragments while keeping `solidity_ast_valid` and deployability false | Exact 30-row overfit outputs recomputed to solidity_valid 1.0 and bytecode semantic score 1.0. Broad-30 improved `solidity_valid_mean` 0.0 -> 0.9 and bytecode semantic score 0.2374 -> 0.4344 without changing generation text | The previous 0% Solidity-valid and low bytecode score were partly evaluator artifacts for function fragments; remaining broad failures are true model behavior errors, not syntax collapse |
| 12. TAC-derived opcode/context segment coverage | `src/training_pipeline.py`, `train.py`, `tests/test_evaluation_quality_metrics.py`, final broad eval `results/eval_1782615778.json`, report `models/qwen2_5_coder_7b_qlora_500_loop_iter12_selector_metadata_100/latest_results_broad30_context_segments.txt` | Persist derived opcode/control-flow slices in eval detail metadata and add common context groups (`calldata`, `caller_context`, `call_value`, `block_context`, etc.) | Broad-30 opcode-group coverage improved from 0/30 in the old report, to 14/30 after passing TAC-derived rare groups, to 27/30 after common context groups; prompt truncation remained 0/30 | Segment conclusions are now more trustworthy. Weakest measured opcode slices are code-introspection/external-call/gas rows (n=2, replication F1 ~0.528, bytecode score ~0.359), while missing facts remain dominated by calls, state writes, guards, returns, and events |
| 13. Repetition-penalty decode sweep | `results/eval_1782616279.json`, `results/eval_1782616380.json`, `results/eval_1782616492.json` compared to `results/eval_1782615778.json` | Same model and broad-30 split, greedy decoding, `eval_max_new_tokens=512`, repetition penalties 1.0, 1.05, 1.10, and previous 1.15 | 1.05 was best: semantic 0.7055 vs 0.6933, edit distance 0.5822 vs 0.5937, replication F1 micro 0.6076 vs 0.5954, solidity valid 93.33% vs 90%, bytecode semantic 0.4569 vs 0.4344 | Lowering repetition penalty modestly improves behavior and syntax on a comparable 30-row split; set evaluation default to 1.05 |
| 14. Failure-slice tooling and 100-step checklist | `scripts/analyze_eval_failures.py`, `tests/test_eval_failure_analysis.py`, `data/eval_failure_slices/broad30_reppenalty_1_05/slice_manifest.json` | Analyzed best broad-30 eval and materialized ranked JSONL slices plus a deterministic 100-iteration checklist | Top actionable slices: calls (23 rows, F1 ~0.580 from broad details), selectors (22), unsupported ABI/calls (20 each), calldata (20), state writes (17), guards (17), events (11), returns (11) | Future loops should use fixed failure slices instead of broad-only averages; calls and state writes are the first curriculum targets |
| 15. Calls slice eval | `results/eval_1782616677.json`, dataset `data/eval_failure_slices/broad30_reppenalty_1_05/calls.jsonl` | 23-row calls slice, same model, greedy, `eval_max_new_tokens=512`, default repetition penalty 1.05 | Semantic 0.6662, edit distance 0.6403, replication F1 micro 0.5422, bytecode semantic 0.4103, solidity valid 91.3%; missing call facts=43 and unsupported calls=36 | Calls/member calls are the clearest first failing capability; broad improvements will likely be capped until this slice improves |
| 16. State-write slice eval | `results/eval_1782616749.json`, dataset `data/eval_failure_slices/broad30_reppenalty_1_05/state_writes.jsonl` | 17-row state-write slice, same model, greedy, `eval_max_new_tokens=512`, default repetition penalty 1.05 | Semantic 0.7017, edit distance 0.6639, replication F1 micro 0.5399, bytecode semantic 0.3763, solidity valid 88.2%; missing state_write=18 and invented_state_writes=15 | State writes are intertwined with calls/guards and should be the second curriculum target after calls |
| 17. Inference default alignment | `src/inference.py`, `src/model_setup.py`, `scripts/decompile.py`, `web/app.py`, `web/static/app.js`, `tests/test_decompile_cli.py` | Propagated the measured 1.05 repetition penalty from eval into direct model inference, CLI defaults, web API defaults, and browser UI defaults | Targeted CLI/web tests pass, and generated configs now carry `repetition_penalty=1.05` unless explicitly overridden | The best measured decode setting is no longer limited to `train.py` eval runs; ad hoc inference and UI trials should be comparable |
| 18. Non-overlap curriculum builder | `scripts/build_curriculum_dataset.py`, `tests/test_build_curriculum_dataset.py`, `data/curriculum/calls48_nonoverlap.jsonl`, `data/curriculum/state_writes48_nonoverlap.jsonl` | Built deduplicated call/state-write curriculum sources from `data/hf_training_dataset.jsonl`, excluding the broad-30 eval body hashes and capping inputs at 9k chars/outputs at 2.5k chars | Calls curriculum: 48 unique body hashes, focus facts min 18/max 40/avg 21.92. State-write curriculum: 48 unique body hashes, focus facts min 9/max 51/avg 15.06 | The next retrain can target the known bottlenecks without leaking the fixed broad-30 eval rows or duplicating optimizer variants of the same body |
| 19. Calls-curriculum continuation rejected | `results/eval_1782617808.json`, `results/eval_1782617880.json`, `results/eval_1782617977.json`, `train.py`, `tests/test_train_cli_issues.py` | Continued from selector-metadata checkpoint-210 on 48 call-heavy rows for 30 extra effective steps (`max_steps=240`, LR 1e-4, LoRA rank 32/alpha 64/dropout 0) and evaluated fixed calls/state slices | Calls slice: semantic 0.6662 -> 0.6726 and bytecode 0.4103 -> 0.4148, but replication F1 fell 0.5422 -> 0.5312 and recall fell 0.5056 -> 0.4944. State-write slice regressed on semantic, F1, bytecode, and validity. The generated model was deleted. | A dense calls curriculum alone is not enough and may overfit toward precision at the expense of recall; future continuation needs a heldout curriculum split, lower LR/steps, or mixed call+state+negative examples |
| 20. Mixed continuation rejected after larger holdout | `data/curriculum_eval/calls_state64_holdout_nonoverlap.jsonl`, `results/eval_1782618265.json`, `results/eval_1782618328.json`, `results/eval_1782618444.json`, `results/eval_1782618963.json`, `results/eval_1782619383.json` | Continued from selector-metadata checkpoint-210 on 64 mixed call/member-call/state-write/guard rows for 10 extra effective steps (`max_steps=220`, LR 5e-5). It looked mildly positive on broad-30/calls-23/state-17, then was checked on a separate 64-row mixed holdout excluding broad and training body hashes. | Smoke slices: broad F1 0.6076 -> 0.6095, calls F1 0.5422 -> 0.5444, state F1 unchanged. Larger 64-row holdout rejected it: F1 0.8618 -> 0.8596, bytecode 0.8055 -> 0.8045, precision/recall both down despite tiny semantic/edit gains. The kept-model commit was reverted and the generated model was deleted. | Tiny fixed slices can produce false-positive gains; require a larger non-overlap holdout before keeping curriculum continuations |
| 21. Paired/suite gate and negative curriculum setup | `scripts/compare_eval_runs.py`, `scripts/eval_gate_suite.py`, `data/curriculum_negative/no_calls64_nonoverlap.jsonl`, `data/curriculum_negative/no_state_writes64_nonoverlap.jsonl`, session reports `mixed_gate_suite.*` and `calls_only_gate_suite.*` | Added paired eval comparison, required multi-slice suite gating, and curriculum selection filters that can require absent extracted fact categories. Re-ran gates on both rejected continuations. | Calls-only suite rejected calls F1 -0.0110 and state-transfer F1 -0.0133/bytecode -0.0079/semantic -0.0210/valid -0.0588. Mixed suite rejected state semantic -0.0003 and holdout64 F1 -0.0022/bytecode -0.0010 despite broad/calls upticks; paired holdout comparison showed storage-write mismatches +7 and invented state writes +7. Negative datasets each have 64 non-overlap rows with zero extracted required facts. | Future model commits should be gated by broad, calls, state, and 64-row holdout together. Negative-example curricula can now target unsupported calls and invented state writes without leaking fixed eval rows. |
| 22. Negative-mixed continuation rejected | Baseline negatives `results/eval_1782620381.json`, `results/eval_1782620682.json`; candidate negatives `results/eval_1782621875.json`, `results/eval_1782622164.json`; session train mix `negative_mixed64_train_iter104.jsonl`; gate report `negative_holdout_gate_iter104.*` | Built a 64-row session-only mix with 16 calls, 16 state writes, 16 no-call negatives, and 16 no-state-write negatives. Continued from checkpoint-210 for 10 optimizer steps (`max_steps=220`, LR 2e-5, LoRA rank 32/alpha 64/dropout 0), then evaluated the two negative holdouts before full suite gating. | No-call holdout improved slightly: F1 +0.0025, bytecode +0.0007, semantic +0.0067, valid +0.0156, and unsupported calls -3. No-state holdout rejected the candidate: F1 -0.0017, bytecode -0.0012, semantic -0.0013, valid -0.0156 despite unsupported calls -4 and invented state writes -1. The generated model was deleted. | Negative rows reduce some unsupported buckets but still trade away correctness and validity; do not proceed to full broad/calls/state/holdout gate when a target negative holdout already regresses. |
| 23. Simple negative controls | `data/curriculum_negative/no_calls_simple64_nonoverlap.jsonl`, `data/curriculum_negative/no_state_writes_simple64_nonoverlap.jsonl`, baseline evals `results/eval_1782622383.json`, `results/eval_1782622484.json` | Added `--max-focus-facts` to the curriculum builder and rebuilt no-call/no-state controls with `focus_total=5` rather than the earlier ABI-heavy 11-17 range. Evaluated the current accepted adapter on both simple slices. | Simple no-call vs heavy no-call: F1 0.6362 vs 0.4705, bytecode 0.4764 vs 0.3251, selector mismatches 152 vs 586, unsupported calls 43 vs 71. Simple no-state vs heavy no-state: F1 0.5701 vs 0.5002, bytecode 0.4313 vs 0.3275, selector mismatches 151 vs 574, but unsupported calls remain high at 117 and invented state writes 41. | The earlier negative slices were confounded by very complex ABI targets. Use the simple negative controls for hallucination-focused prompt/decoding/curriculum checks. |
| 24. Simple-negative decode sweep rejected | `results/eval_1782622642.json`, `results/eval_1782622742.json`, `results/eval_1782622846.json`, `results/eval_1782622949.json`, `results/eval_1782623055.json`, `results/eval_1782623161.json`; session reports `simple_negative_256_gate.*`, `simple_negative_rp110_gate.*`, `simple_negative_rp115_gate.*` | Compared `eval_max_new_tokens=256` vs 512 and repetition penalties 1.10/1.15 vs the 1.05 baseline on the simple no-call/no-state controls. | 256 tokens was identical to 512 on both slices, so max-new-token overgeneration is not the driver. Repetition 1.15 improved no-call F1 +0.0082/bytecode +0.0080 but regressed no-state F1 -0.0356/bytecode -0.0577/valid -0.0313. Repetition 1.10 regressed both simple slices. | Keep 1.05 as the decode default; unsupported calls/state on simple negatives require model/data changes, not a max-token or repetition-penalty tweak. |
| 25. Prompt grounding hint rejected | Candidate prompt evals `results/eval_1782623346.json`, `results/eval_1782623440.json`; session report `simple_negative_grounding_hint_gate.*` | Temporarily added an inference/training prompt hint: only include calls, storage writes, events, guards, and returns supported by TAC/metadata. Evaluated the accepted adapter on both simple negative controls, then reverted the prompt change. | The hint reduced simple no-state unsupported calls 117 -> 98 and invented state writes 41 -> 39, but regressed no-call F1 -0.0068/bytecode -0.0043 and no-state F1 -0.0034/bytecode -0.0252/valid -0.0313; it also increased invented events on both slices. | Prompt-only grounding constraints are not safe for the current adapter; they reduce some hallucination buckets but damage correctness and validity. |
| 26. Pure call/state negative control, superseded by iteration 27 | `data/curriculum_negative/no_calls_no_state_simple64_nonoverlap.jsonl`, eval `results/eval_1782623579.json` | Built a 64-row simple control with zero extracted `call`, `member_call`, and `state_write` facts, `focus_total=5`, and no overlap with broad/holdout gates. Evaluated the current accepted adapter. | Initial metrics were F1 0.6162, bytecode 0.4454, semantic 0.7174, valid 0.8906 with unsupported calls=74 and invented state writes=30. | Superseded: nested mapping state writes were under-extracted, so the dataset and eval were rebuilt in iteration 27. |
| 27. Nested mapping state-write extractor fix | `src/replication_metrics.py`, `tests/test_replication_metrics.py`, rebuilt `data/curriculum_negative/no_state_writes64_nonoverlap.jsonl`, `data/curriculum_negative/no_state_writes_simple64_nonoverlap.jsonl`, `data/curriculum_negative/no_calls_no_state_simple64_nonoverlap.jsonl`, eval `results/eval_1782623806.json` | Fixed state-write fact extraction for nested mapping assignments like `allowed[msg.sender][spender] = amount` and `freemintAddresses[addresses[i]] = true`; rebuilt affected zero-state datasets. | The accepted 83-row train split has 20 pure no-call/no-state rows after the fix (previously counted as 22), and the full source has 5,523 pure rows (previously 5,986). Corrected pure-negative eval: F1 0.5955, bytecode 0.4216, semantic 0.6938, valid 0.8750, unsupported calls=74, invented state writes=36. | Earlier no-state/pure-negative evals undercounted nested mapping writes and should be treated as stale. The hallucination problem remains after correcting the labels. |
| 28. Post-fix simple-negative baselines | `results/eval_1782623967.json`, `results/eval_1782624064.json` | Recomputed the simple no-call and simple no-state baselines after the nested mapping state-write fix so future comparisons use the corrected evaluator. | Corrected simple no-call: F1 0.6355, bytecode 0.4763, semantic 0.7360, valid 0.9375, unsupported calls=43, invented state writes=27. Corrected simple no-state: F1 0.5701, bytecode 0.4313, semantic 0.7182, valid 0.9375, unsupported calls=117, invented state writes=41. | Use these post-fix JSONs, not the earlier simple-negative baseline evals, for future gates. |
| 29. Post-fix core gate baselines | `results/eval_1782624189.json`, `results/eval_1782624247.json`, `results/eval_1782624305.json`, `results/eval_1782624708.json` | Recomputed the accepted adapter on broad-30, calls-23, state-17, and calls_state64 holdout after the nested mapping state-write extractor fix. | Corrected broad-30: F1 0.6049, bytecode 0.4554, semantic 0.7055, valid 0.9333. Calls-23: F1 0.5389, bytecode 0.4083. State-17: F1 0.5355, bytecode 0.3736. Holdout64 remained F1 0.8618, bytecode 0.8055, valid 1.0. | Use these corrected core baseline IDs for future suite gates; pre-fix F1 values are slightly optimistic for broad/calls/state. |
| 30. Post-fix broad failure slices | `data/eval_failure_slices/broad30_reppenalty_1_05_fixed_extractor/slice_manifest.json`, session reports `broad30_fixed_failure_analysis.*` | Regenerated broad-30 failure slices from corrected eval `results/eval_1782624189.json`. | Top mismatches: call=98, selector=64, storage_write=36, guard=29. Top hallucinations: unsupported_calls=36, unsupported ABI=35, invented_state_writes=15. Top missing facts: call=43, state_write=21, member_call=19. Ranked fixed slices keep calls and state_writes as core bottlenecks. | Future slice evals/curricula should use `broad30_reppenalty_1_05_fixed_extractor` instead of the pre-fix slice directory. |
| 31. Larger-run decision checkpoint | `learnings/learnings.md`, corrected gates `results/eval_1782624189.json`, `results/eval_1782624247.json`, `results/eval_1782624305.json`, `results/eval_1782624708.json`, negatives `results/eval_1782623806.json`, `results/eval_1782623967.json`, `results/eval_1782624064.json`, runner `run_train_qwen_qlora_500.sh` | Reviewed whether the accumulated evidence justifies longer/larger Qwen QLoRA training. No new model was trained. Current full source has 31,577 rows; accepted adapter was trained on a 100-row sample / 83 train rows with selector metadata, 10 epochs, 4 GPUs, global batch 4, rank 32/alpha 64/dropout 0, and `TRAIN_EVAL_STRATEGY=no`. | Scaling is now justified, but only as a staged controlled experiment rather than a blind large run. Broad/calls/state fixed gates are still low (broad F1 0.6049/bytecode 0.4554; calls F1 0.5389/bytecode 0.4083; state F1 0.5355/bytecode 0.3736), while holdout64 is much higher (F1 0.8618/bytecode 0.8055) and pure-negative hallucinations remain high (74 unsupported calls, 36 invented state writes). Prompt truncation remains 0 and decode has already been swept, so the next uncertainty is data scale/conditioning. | Run a staged 300-row selector-metadata experiment first and promote to 1k+ only if fixed gates and negative controls improve together. Keep paired train/heldout evals and the suite gate mandatory to avoid broad-only false positives. |
| 32. 300-row staged scale-up rejected | model `models/qwen2_5_coder_7b_qlora_500_loop_iter32_selector_metadata_300/`, sample `data/qwen_qlora_500_loop_iter32_selector_metadata_300/qwen_qlora_300_sample.jsonl`, built-in test `results/eval_1782685781.json`, train-first-30 `results/eval_1782685917.json`, suite `models/qwen2_5_coder_7b_qlora_500_loop_iter32_selector_metadata_300/gate_suite_iter32.md` | Fresh 300-row sampled run, 250 train / 33 val / 17 test, 12 epochs / 756 steps, 4 GPUs, global batch 4, LoRA rank 32/alpha 64/dropout 0, selector metadata enabled, `TRAIN_EVAL_STRATEGY=no`, `EVAL_MAX_NEW_TOKENS=512`, repetition penalty 1.05. | Train-first-30 memorized strongly: F1 0.9699, semantic 0.9885, bytecode 0.8965, strict signatures 30/30. Built-in test17 remained smoke-only and weak: F1 0.4924, bytecode 0.3638, strict signatures 11/17, hallucination rate 50%. Suite rejected candidate: broad30 F1 -0.0119 despite bytecode +0.0066; holdout64 bytecode -0.0527 and valid -0.0156; large192 valid -0.0156. Calls23, state17 bytecode, and pure-negative improved locally, but not enough. | Do not promote random 300-row scale-up to 1k. The pipeline still memorizes, but random scale alone does not improve robust TAC-grounded generalization and can trade recall/validity against local slice gains. |
| 33. Large holdout and fallback curriculum | large holdout `data/curriculum_eval/large_stratified192_nonoverlap_iter32.jsonl`, baseline `results/eval_1782687160.json`, candidate `results/eval_1782687640.json`, fallback curriculum `data/curriculum/fallback_balanced300_iter32.jsonl`, manifest `data/curriculum/fallback_balanced300_iter32_manifest.json` | Built a 192-row non-overlap behavior holdout excluding the 300-row sample and fixed gates, then evaluated the accepted 100-row adapter and the rejected 300-row adapter. After rejection, built a 300-row balanced fallback curriculum excluding all gates and the random 300 sample. | Large192 baseline: F1 0.3980, bytecode 0.2923, semantic 0.6414, valid 0.9219. Large192 candidate: F1 0.4023, bytecode 0.2972, semantic 0.6429, valid 0.9062, so the gate rejected on validity. Fallback curriculum contains 60 pure-negative, 80 calls+state+guards, 50 calls, 50 state-write, 30 return, and 30 event rows; coverage includes 204 call/member-call, 197 state-write, 190 guard/modifier, 91 return, 81 event, and 75 pure no-call/no-state rows. | Use the fallback balanced curriculum as the next controlled data-conditioning run before any 1k random scale-up. Keep large192 as a required gate because it exposed validity regression while small slices looked mixed-positive. |
| 34. Full-run readiness assessment | `data/hf_training_dataset.jsonl`, this file | Assessed whether the accumulated source data is large enough for a full QLoRA run and whether remaining blockers are training/eval bugs. Dataset audit: 31,577 rows, 16,759 unique `metadata.body_hash` values, 14,818 duplicate-body rows, 100% selector and function-signature coverage. Behavior coverage is broad: calls in 18,139 rows, member calls in 12,099, state writes in 15,507, guards in 11,656, returns in 13,784, events in 5,866, and pure no-call/no-state in 5,523. | The dataset is large enough to justify a full run in row count and behavior coverage, but raw full-dataset training should account for heavy duplicate bodies and gate against hallucination/validity regressions. No current evidence says core training/eval wiring is broken: overfits memorize, selector metadata works, prompt truncation is repeatedly zero, and adapter save/load is exercised. Remaining evaluation issues are mostly known limitations: deployability/AST are context-limited for function fragments, runtime matching is not active, and small slices can mislead. | It is reasonable to “rip the band-aid off” with a full-data run, but use a deduplicated or body-balanced source and the large192/core/negative suite as gates. Do not treat full-run success as proven unless heldout behavior improves, not just training loss. |
| 35. Full body-balanced runner script | `run_train_qwen_qlora_full_body_balanced.sh` | Added a dedicated full-run script that materializes a `metadata.body_hash`-capped dataset (`CAP_PER_BODY=1` by default), launches Qwen QLoRA with 1 epoch, 4 GPUs, global batch 4, LoRA rank 32/alpha 64/dropout 0, selector metadata enabled, no train-time eval, `eval_max_new_tokens=512`, repetition penalty 1.05, and then runs the current gate suite. | Smoke dry-run with `MAX_ROWS=20 DRY_RUN=1 RUN_GATES=0` successfully built a 20-row body-balanced dataset outside the repo and printed the expected training command. The script reports source rows, unique body identities, duplicate-body rows, selected rows, behavior category counts, compiler distribution, and gate artifact paths. | Use this script for the first full body-balanced run. Override `CAP_PER_BODY`, `MAX_ROWS`, `RUN_ID`, and `RUN_GATES` as needed; keep `CAP_PER_BODY=1` for the cleanest deduplicated first pass. |
| 36. Default full body-balanced run rejected | dataset `data/qwen_qlora_full_body_balanced_20260628-162859/body_balanced_dataset.jsonl`, output `models/qwen2_5_coder_7b_qlora_full_body_balanced_20260628-162859/`, built-in test `results/eval_1782703089.json`, train-first-30 `results/eval_1782703171.json`, gate report `models/qwen2_5_coder_7b_qlora_full_body_balanced_20260628-162859/eval_gates/gate_suite.md` | Ran `./run_train_qwen_qlora_full_body_balanced.sh` in default mode. Dataset: 16,759 selected rows from 31,577 source rows, capped at one row per body identity; split 13,635 train / 2,077 val / 1,047 test. Settings: 1 epoch, 4 GPUs, global batch 4, LoRA rank 32/alpha 64/dropout 0, selector metadata enabled, `TRAIN_EVAL_STRATEGY=no`, `EVAL_MAX_NEW_TOKENS=512`, repetition penalty 1.05. Training completed in 12,062.98s with 3,409 optimizer steps and train loss 0.7058. | Built-in 1,047-row test: failure 0%, semantic 0.7840, edit distance 0.4290, F1 micro 0.6612 (precision 0.6828, recall 0.6409), bytecode 0.5425, Solidity valid 0.9618, AST/deployable 0.0162, prompt truncation 0. Train-first-30 did not memorize: F1 0.6087, semantic 0.7764, bytecode 0.5133. Gate suite rejected: broad30/calls23/holdout64/large192 all improved F1/bytecode/semantic but regressed `solidity_valid_mean`; state17 and pure_negative64 kept. Large192 improved F1 +0.0449 and bytecode +0.0345 but validity dropped 0.9219 -> 0.8750. Pure-negative hallucinations improved (unsupported calls 74 -> 29, invented state writes 36 -> 17), but large192 shifted errors toward invented guards, invented state writes, and unsupported returns. | Do not accept the final full body-balanced adapter. The full run is useful evidence that scale/body balancing improves behavior metrics and pure-negative grounding, but 1 epoch underfits train rows and introduces syntax/scaffold validity regressions. Next cheap check is evaluating checkpoint-3000 or a syntax-aware/lower-risk continuation before retraining from scratch. |
| 37. Checkpoint-3000 gate rejected | eval-ready checkpoint `models/qwen2_5_coder_7b_qlora_full_body_balanced_20260628-162859/eval_checkpoint_3000/`, gate report `models/qwen2_5_coder_7b_qlora_full_body_balanced_20260628-162859/eval_gates_checkpoint_3000/gate_suite.md`, evals `results/eval_1782705516.json`, `results/eval_1782705611.json`, `results/eval_1782705682.json`, `results/eval_1782705733.json`, `results/eval_1782706119.json`, `results/eval_1782706195.json`, `results/eval_1782706796.json` | Materialized checkpoint-3000 with final model tokenizer/config and ran train-first30 plus six-slice gate suite. Also added reusable runner `run_eval_gate_suite_for_model.sh` for future checkpoint/decode sweeps. | Broad30, calls23, and state17 all improved F1/bytecode/semantic while preserving target-compiler validity: broad F1 +0.0598/bytecode +0.0391, calls F1 +0.0684/bytecode +0.0375, state F1 +0.1186/bytecode +0.1016. Pure-negative64 improved strongly (F1 +0.1169, bytecode +0.1142, valid +0.0156). However holdout64 rejected on valid -0.0469 with one scaffold-invalid 512-token runaway on `bite(bytes32,address)`, and large192 rejected on valid -0.0365 with 9 malformed scaffold-invalid cap rows (baseline 1, final 11). Train-first30 remained weak: F1 0.6045, bytecode 0.5129. | Checkpoint-3000 is better than the final checkpoint on most small/medium slices and has fewer large192 runaways than final, but it is still not acceptable. The failure is a real repetition/runaway validity problem, not just target compiler incompatibility, and checkpoint selection alone does not solve underfit or scaffold robustness. |
| 38. Full-run gate contamination found and runner fixed | overlap audit, `run_train_qwen_qlora_full_body_balanced.sh`, dry-run dataset `data/qwen_qlora_full_body_balanced_eval_clean_dry_run_manifest_20260628-212523/body_balanced_dataset.jsonl` | Audited fixed gate body hashes against the default full body-balanced split, then patched the full-run script to exclude fixed gate datasets before body balancing by default via `EVAL_EXCLUDE_DATASETS`. Validated with `bash -n` and a dry run. | The rejected full run trained on most fixed gate rows: broad30 24 train/5 val/1 test, calls23 18/4/1, state17 16/1/0, holdout64 61/1/2, pure-negative64 49/15/0, and large192 154/34/4. The clean dry-run preserved source stats (31,577 rows, 16,759 identities), excluded 390 eval dataset rows, 350 unique eval identities, and 623 source rows, then selected 16,409 body-balanced rows with zero overlap against broad30/calls23/state17/holdout64/pure-negative64/large192. | Treat full-run gate improvements as contaminated evidence until rerun with eval-clean training data. The next full E2E run should use the patched runner defaults so fixed gates remain true heldouts. |
| 39. Checkpoint-3000 1024-token decode sweep rejected | decode dir `models/qwen2_5_coder_7b_qlora_full_body_balanced_20260628-162859/eval_decode_1024_checkpoint_3000/`, evals `results/eval_1782707568.json`, `results/eval_1782707988.json` | Re-evaluated checkpoint-3000 on large192 and holdout64 with `eval_max_new_tokens=1024` and repetition penalty 1.05. | Large192 kept the same target-compiler validity as 512 (0.8854) and the same 9 scaffold-invalid rows, but every malformed row ran to 1024 tokens instead of 512; F1/bytecode were slightly lower (F1 0.4448 -> 0.4437, bytecode 0.3291 -> 0.3287). Holdout64 also kept identical metrics and validity (F1 0.8797, bytecode 0.8295, valid 0.9531), with the same `bite(bytes32,address)` scaffold failure extended from 512 to 1023 tokens. Prompt truncation remained 0. | Raising max-new-tokens does not fix the runaway/scaffold issue and roughly doubles bad-row generation cost. Keep 512 as the default gate budget for now; the runaway fix needs model/data conditioning or an explicit safe stopping strategy, not a larger decode cap. |
| 40. Eval-clean full body-balanced run in progress | dataset `data/qwen_qlora_full_body_balanced_eval_clean_20260628-214054/body_balanced_dataset.jsonl`, output `models/qwen2_5_coder_7b_qlora_full_body_balanced_eval_clean_20260628-214054/`, log `/tmp/full_body_balanced_eval_clean_20260628-214054.log` | Launched the patched full body-balanced runner with default gate exclusions and 512-token eval budget. | Dataset manifest: 31,577 source rows, 16,759 source identities, 350 excluded eval identities, 623 excluded source rows, 30,954 available rows, 16,409 available/selected body-balanced identities. Training started successfully on 4 GPUs with 3,341 optimizer steps; startup progress reached 68/3341 (~2%) with loss already down to ~0.8054 after the first logged intervals. | In progress. This is the first full run whose fixed gate suite should be treated as true heldout evidence. Compare final and any checkpoint candidates against the same gate suite, but interpret 512-token runaways as real scaffold failures. |
| 41. Raw full-source 1-epoch run via sampled runner | dataset `data/qwen_qlora_500_full_raw_all_1epoch_20260702-000545/qwen_qlora_31577_sample.jsonl`, split `data/qwen_qlora_500_full_raw_all_1epoch_20260702-000545/splits/`, model `models/qwen2_5_coder_7b_qlora_500_full_raw_all_1epoch_20260702-000545/`, built-in eval `results/eval_1783000992.json`, train-first30 `results/eval_1783001111.json`, gates `models/qwen2_5_coder_7b_qlora_500_full_raw_all_1epoch_20260702-000545/eval_gates_raw_full_1epoch/` | User-requested all-source run with `run_train_qwen_qlora_500.sh`, `SAMPLE_COUNT=31577`, 1 epoch, 4 GPUs, global batch 4, LoRA rank 32/alpha 64/dropout 0, selector metadata enabled, `TRAIN_EVAL_STRATEGY=no`, `eval_max_new_tokens=512`, repetition penalty 1.05. Split: 25,723 train / 3,964 val / 1,890 test; 6,431 optimizer steps. | Built-in 1,890-row test: failure 0%, semantic 0.7734, edit 0.4496, F1 micro 0.6281 (precision 0.6463, recall 0.6109), bytecode 0.5083, Solidity valid 0.9619, deployable/AST 0.0243, hallucination rate 35.37%, groundedness 0.6782, prompt truncation 0. Train-first30 was not memorized: F1 0.7994, semantic 0.8892, bytecode 0.6698. Top issues: call mismatches 5,504, selector 3,446, guard 2,666, compiler/deployability 1,844, state-write 1,667; top hallucinations unsupported calls 2,193, unsupported ABI 1,646, invented guards 1,349. The diagnostic gate suite passed, but every fixed gate overlapped the raw split: broad30 26 train/2 val/2 test, calls23 20/1/2, state17 16/1/0, holdout64 63/0/1, pure-negative64 57/3/4, large192 162/21/9. | Raw duplicate-heavy all-row training is not accepted as a model improvement. It confirms scale helps fixed/overlapping gates, but it underperforms the earlier body-balanced full run on the built-in heldout metrics and makes fixed gates unusable as clean evidence. Prefer eval-clean body-balanced/deduplicated full runs for acceptance. |
| 41. Single-compiler data-generation policy | `download_hf_contracts.py`, `src/local_compiler.py`, `src/dataset_pipeline.py`, `train.py`, `README.md`, `docs/runbook.md`, tests `tests/test_data_generation_export_issues.py`, `tests/test_dataset_quality_issues.py`, `tests/test_docs_consistency.py` | Reviewed whether training-data generation should compile each source across three compatible compiler versions. No model was trained; this was a design/code correction using prior dataset/gate evidence. | Prior dataset evidence shows 31,577 rows but only 16,759 unique bodies and 14,818 duplicate-body rows, and successful full-run scripts already body-balance with `CAP_PER_BODY=1`. Multi-version/optimizer expansion mostly reweights identical Solidity targets, consumes compile budget, and conflicts with source-aligned verified compiler metadata. | Training-data generation now selects one source-aligned compiler/optimizer config per contract, falling back to one compatible config only when verified compiler metadata is missing/unusable. The old `--max-compiler-versions` / `--max-compiler-configs` knobs are compatibility no-ops, not augmentation controls. |
| 42. Full fresh data rebuild + 4-GPU QLoRA run in progress | rebuild log `/tmp/full_rebuild.log`, dataset `data/hf_training_dataset.jsonl`, prefilter copy `data/hf_training_dataset_prefilter.jsonl`, splits `data/{train,val,test}_dataset.jsonl`, selectors `data/selectors.json`, training log `/tmp/train_fresh.log`, output `models/qwen_qlora_full_fresh_20260801-140727/` | Deleted `data/contracts.db` and all exports, then re-ran the full pipeline with `--limit 0 --workers 32 --max-body-dupes 2 --max-seq-length 8192`. Download stored 109,144 contracts (vs 24,898 previously). Compile phase ran 107,299 jobs in ~59 min with 16.6% solc errors, producing 72,709 function pairs. Export wrote 31,121 rows (3,559 quarantined) and 302,474 unique selectors (vs 69,202 previously). Preflight then failed with 2,326 train rows over the 8,192-token budget, so the source was refiltered by true Qwen tokenizer counts to 28,403 rows (max total 8,190 tokens); resplit gave 23,295 train / 3,406 val / 1,702 test with preflight exit 0. Training launched via `torchrun --nproc_per_node=4` with batch 1, global batch 4, seq 8192, fp16, `--quantization`, gradient checkpointing, 3 epochs, lr 2e-4, `--train-eval-strategy no`. | Data gate passed: 28,403 rows, 24,797 unique normalized bodies, 28,403 unique final rows, zero duplicates. Selector coverage rose 4.4x. Unique-body count rose from 16,759 to 24,797 (+48%). Training reports 17,472 optimizer steps at roughly 7.4 s/step (~36 h projected); 40,370,176 trainable LoRA params (0.53%). Final metrics pending. | Two reusable findings. First, the exporter's `_token_count_estimate` heuristic (`max(whitespace, chars/4)`) under-measures TAC relative to the real Qwen tokenizer, so `--max-seq-length` filtering at export does not guarantee preflight-clean rows; filter the source with the actual tokenizer via `train._preflight_prompt_parts` before splitting. Second, single-process `train.py` runs with 4 visible GPUs are wrapped in DataParallel and crash with a device-side assert in `nll_loss` (non-quantized) or an illegal memory access (quantized); pin `CUDA_VISIBLE_DEVICES=0` for single-GPU smoke tests and use torchrun/DDP for multi-GPU. |

| 43. Mid-run CPU smoke eval of checkpoint-11500 (fresh-data run) | checkpoint `models/qwen_qlora_full_fresh_20260801-140727/checkpoints/checkpoint-11500/`, staged eval dir `/tmp/ckpt11500_eval`, eval `results/eval_1785710884.json`, log `/tmp/cpu_smoke_eval.log` | While the 4-GPU run was still training (step ~11,836/17,472, epoch 2.03), all four GPUs held 42.8-47.7 GiB of 48 GiB, leaving too little headroom for a GPU eval without risking an OOM in the live trainer. Ran the eval entirely on CPU instead with `CUDA_VISIBLE_DEVICES=""`, `--no-quantization --precision fp32`, 15 first-N test rows, `eval_max_new_tokens=512`, repetition penalty 1.05. Staging the adapter required copying base tokenizer/config files next to `adapter_model.safetensors`, otherwise preflight fails with `Unrecognized model ... model_type`. | 15/15 succeeded, failure rate 0. Semantic 0.8557, normalized edit 0.2815, replication F1 micro 0.8112 (precision 0.8467, recall 0.7785), bytecode semantic 0.6901 (checked 1.0), Solidity valid 1.0, AST/deployable 0.0667, signature match 0.80, visibility match 0.8667, structural preservation 0.8967. Prompt truncation 0; prompt tokens 608-1181 vs 8192 window; generated tokens 19-114 with zero rows hitting the 512 cap. Split audit: 0/1702 test rows share an input or output hash with train, but test itself holds only 1,314 unique outputs across 1,702 rows, so duplicates cluster inside the split (group-aware splitting working as intended). | Directionally the strongest heldout numbers this repository has recorded (F1 micro 0.8112 vs 0.6612 best prior, bytecode 0.6901 vs 0.5425), but n=15 is smoke-only per the 30-sample rule and the first-N slice contained 5 duplicate-output pairs, so it measures roughly 10 distinct targets. Do not treat as acceptance evidence. Also discovered the run was launched WITHOUT LoRA flags and silently used train.py defaults r=16/alpha=32/dropout=0.1 instead of the intended r=32/alpha=64/dropout=0. |

## Durable learnings

1. **The overfit sanity check is necessary and should run without train-time eval.** The earlier 10-row run stopped at epoch 9 because trainer eval enabled early stopping and selected an earlier checkpoint. Disabling train-time eval via `TRAIN_EVAL_STRATEGY=no` allowed the corrected 10-row run to reach 30 epochs and memorize exactly.
2. **All QLoRA scripts should use 4 GPUs by default for these experiments.** `run_train_qwen_qlora_500.sh` already defaulted to `NUM_GPUS=4`; `run_train_qwen_qlora_overfit_sanity_check.sh` was updated to default to `NUM_GPUS=4` and `GLOBAL_BATCH_SIZE=4`.
3. **Prompt truncation is not the current primary failure mode.** The 300-row evals and the 100-row iteration-4 evals reported zero prompt truncation. Iteration 4 prompt diagnostics had heldout prompt tokens mean ~1574 and max ~3243 under an 8192-token context.
4. **Standalone Solidity validity and bytecode deployability understate memorization quality for function fragments.** Exact-match overfit outputs can still fail deployability because the evaluator compiles fragments without required contract state declarations. Treat syntax/deployability as separate from same-row memorization until the harness understands contract context.
5. **The old `function_signature_match` metric was invalid.** It only compared whether both strings contained a function keyword. This falsely gave `function_signature_match_mean=1.0` to outputs with wrong names like `setMaxTxAmount`, `setBaseURI`, or `getReward`. The strict signature metric now compares function name, parameter count/types, and return count/types.
6. **The model can memorize but is not yet learning robust TAC-conditioned generalization.** The corrected 10-row and 30-row overfits show exact same-row fit, while 300-row and 100-row heldout results still generate unrelated but plausible Solidity functions.
7. **Heldout sample counts below 30 are smoke signals.** The 100-row run had only 6 heldout test examples, so its heldout metrics should not be overinterpreted. Its train-row eval is still useful for memorization diagnostics.
8. **Selector signatures are a major recoverable context feature.** The local selector registry recovered 100% of the iteration-4 split signatures, and adding `selector_signature=...` to prompt metadata improved a 30-row non-overlap eval from 0 to 19 strict signature matches without retraining.
9. **Selector metadata does not solve bytecode-grounded semantics by itself.** The retrained selector-metadata model improved broad-30 replication F1 micro to ~0.595 and signatures to 24/30, but call, storage-write, guard, selector, compiler/deployability, and syntax/scaffold buckets still dominate.
10. **Deployability remains an evaluator/model-context issue for function fragments.** Exact or near-exact train-row outputs still score 0 deployability because function-level snippets are compiled without contract state/context.
11. **`solidity_valid` should mean syntax/scaffold validity for function fragments, not full deployability.** Local solc often reports undeclared identifiers or missing modifiers when a correct function body references contract-level state; those are now classified as context-limited compiler results while `bytecode_deployable` remains false.
12. **Evaluation records need TAC-derived segment metadata.** Without copying derived opcode/control-flow groups into per-row metadata, reports can show opcode/control-flow coverage as unknown even when the TAC input contains enough evidence for segmentation.
13. **Use `eval_repetition_penalty=1.05` for current Qwen QLoRA evals.** On the same 30-row broad split it beat 1.0, 1.10, and 1.15 on replication F1 and bytecode semantic score while improving Solidity-valid rate.
14. **Failure-slice evals are now available and should be used before broad retraining.** The calls and state-write slices show lower F1/bytecode scores than broad-30, making them better short-run targets for curriculum and data fixes.
15. **Deduplicate curriculum rows by `metadata.body_hash`.** The source dataset includes optimizer/config variants of the same Solidity body; curriculum selection now keeps one row per body hash and excludes fixed eval body hashes.
16. **Do not keep curriculum continuations based only on broad-30/calls-23/state-17.** The mixed 10-step run looked slightly positive there but regressed on a separate 64-row non-overlap mixed holdout.
17. **Use paired and suite-level gates before keeping model changes.** The new gate correctly rejects both rejected continuations: calls-only regresses calls/state transfer, and mixed regresses state plus the 64-row holdout despite small broad/calls improvements.
18. **Negative curriculum rows should be selected explicitly, not inferred after training.** The curriculum builder can now require categories such as `call`, `member_call`, or `state_write` to be absent, enabling controlled unsupported-call/invented-state-write tests.
19. **Short negative continuations can lower hallucination buckets without improving gated quality.** The negative-mixed run reduced unsupported calls/invented facts on both negative holdouts but still regressed no-state F1, bytecode score, semantic similarity, and validity.
20. **Negative controls should cap ABI/focus complexity.** The first negative slices selected rows with 11-17 ABI-related focus facts and were dominated by selector/ABI mismatches; `max_focus_facts=5` yields cleaner hallucination probes.
21. **Do not change decode defaults based on simple negative slices.** Lowering max new tokens to 256 had no effect, while repetition penalties 1.10 and 1.15 regressed at least one simple negative control.
22. **Prompt-only grounding hints are unsafe for the current adapter.** An explicit "only include supported facts" instruction reduced unsupported calls but regressed F1, bytecode score, and validity on simple negative controls.
23. **The model hallucinates calls and state writes even on pure negative rows.** On corrected 64 simple rows with no target calls/member calls/state writes, the accepted adapter still produced 74 unsupported calls and 36 invented state writes; the earlier pre-fix count was 30.
24. **Nested mapping assignments must count as state writes.** The extractor now catches writes such as `allowed[msg.sender][spender] = amount`; older no-state and pure-negative artifacts before `results/eval_1782623806.json` undercount state writes.
25. **Larger training runs are justified only as staged, gated scale-up.** The accepted 100-row selector-metadata adapter validates the training path but still fails broad/calls/state gates and pure-negative grounding; scale should progress 300 rows -> 1k+ only after fixed gates and negative controls improve together.
26. **A random 300-row selector-metadata scale-up is not enough.** The 300-row run memorized train-first-30 but failed the suite via broad F1, holdout64 bytecode/validity, and large192 validity regressions; do not run random 1k until data conditioning improves.
27. **The accumulated dataset is big enough for a full run, but duplicate-heavy.** `data/hf_training_dataset.jsonl` has 31,577 rows and 16,759 unique body hashes with full selector/signature coverage; train on a deduplicated or body-balanced view rather than assuming every row is independent signal.
28. **One full body-balanced epoch improves behavior metrics but is not acceptable.** The default full run improved F1/bytecode/semantic on every gate slice and strongly improved pure-negative hallucinations, but it failed suite gates on Solidity-valid regressions and did not memorize train-first-30.
29. **Fixed gates must be excluded from full-run training.** The first full body-balanced split included most gate rows in train/val/test (for example, large192 had 154 train + 34 val + 4 test overlaps), so future full runs must reserve gate body hashes before body balancing.
30. **Raw full-source runs through `run_train_qwen_qlora_500.sh` contaminate fixed gates.** The all-row run included every fixed gate body hash somewhere in train/val/test, with most in train, so a passing gate suite after raw full-source training is diagnostic rather than trustworthy heldout evidence.
31. **Duplicate-heavy raw all-row training did not beat body-balanced full training.** The raw 31,577-row run scored lower than the earlier 16,759-identity body-balanced run on comparable built-in test metrics: F1 0.6281 vs 0.6612, semantic 0.7734 vs 0.7840, and bytecode 0.5083 vs 0.5425.
32. **Do not generate training rows by sweeping multiple compiler versions per source.** The prompt is bytecode-only and compiler metadata is oracle-only, while prior data audits show heavy duplicate bodies; source-aligned single compiler configs avoid reweighting identical Solidity targets and preserve compile budget for unique bodies.
33. **Export-time length filtering is not tokenizer-accurate.** `src/dataset_export_primitives._token_count_estimate` uses `max(whitespace_tokens, chars/4)`, which under-measures TAC text; a fresh export capped at `--max-seq-length 8192` still left 2,326/25,507 train rows over budget under the real Qwen tokenizer (max context 17,452 tokens). Refilter the source with `train._preflight_prompt_parts` plus the actual tokenizer before splitting to keep prompt truncation at zero.
34. **Never run single-process `train.py` with multiple GPUs visible.** Hugging Face Trainer wraps the model in DataParallel, which fails with a device-side `nll_loss` assert (`t >= 0 && t < n_classes`) when unquantized and `CUDA error: an illegal memory access` when quantized, despite labels being verifiably in range. `CUDA_VISIBLE_DEVICES=0` makes the same smoke test pass; multi-GPU must use torchrun/DDP.

35. **A mid-training eval can run on CPU when GPUs are saturated.** With all four RTX 8000s at 42-47 GiB of 48 GiB, a 4-bit 7B eval could not fit without risking an OOM in the live trainer. `CUDA_VISIBLE_DEVICES="" --no-quantization --precision fp32` evaluates on CPU at roughly 28 s/sample for short functions, giving zero-risk directional signal. Staged adapter dirs must also contain the base `config.json`/`tokenizer*` files or preflight aborts with `Unrecognized model in <dir>`.
36. **Checkpoint dirs are LoRA adapters only and are not directly evaluable.** `checkpoint-*/` holds `adapter_config.json` + `adapter_model.safetensors` + optimizer/scheduler state, with no tokenizer or base config; copy the base model's `config.json`, `generation_config.json`, `tokenizer.json`, `tokenizer_config.json`, `vocab.json`, and `merges.txt` alongside the adapter before running `--eval-only`.
37. **`train.py` silently falls back to r=16/alpha=32/dropout=0.1 when LoRA flags are omitted.** The fresh-data run `qwen_qlora_full_fresh_20260801-140727` was launched without `--lora-rank/--lora-alpha/--lora-dropout` and trained at half the intended rank with nonzero dropout, unlike every prior successful run (r=32/alpha=64/dropout=0). Always assert LoRA hyperparameters from `training_input_manifest.json` or `adapter_config.json` after launch rather than trusting the command line.
38. **Identical selectors can be genuinely ambiguous, so selector metadata cannot disambiguate every prompt.** `transferFrom(address,address,uint256)` is selector `0x23b872dd` for both ERC20 and ERC721. On that test row the model emitted memorized OpenZeppelin ERC721 boilerplate (`_isApprovedOrOwner`, `tokenId`, no `returns (bool)`) for an ERC20 target, scoring semantic 0.668 / F1 0.583 / bytecode 0.412. This is prior-collapse on the TAC body, not a selector-coverage gap, and refines learning 8: selector metadata is necessary but not sufficient where selectors collide.
39. **Under-generation and truncation are ruled out for this run.** Prompt tokens were 608-1181 against an 8192 window with truncation count 0, and generated tokens ranged 19-114 with no row reaching the 512 cap. Per learning 33's prior, do not spend effort on context-window or decode-budget work for this configuration.

## Hypotheses

1. **High confidence: the 300-row baseline was undertrained for TAC conditioning.** Evidence: it scored poorly even on train rows (`results/eval_1782603004.json`) and emitted repeated unrelated snippets.
2. **High confidence: early stopping can confound overfit sanity checks.** Evidence: the failed 10-row overfit stopped before the requested 30 epochs; the corrected no-train-eval run memorized 10/10.
3. **Medium-high confidence: current data scale/coverage is insufficient for heldout generalization.** Evidence: iteration 4 achieved strong train memorization on many examples but 0/6 strict signature matches on heldout rows, and iteration 5 showed the same pipeline can memorize 30/30 identical train/test rows.
4. **Medium confidence: target/source quality or prompt metadata may be too weak for function identity.** Evidence: the model often generates plausible ABI-compatible Solidity unrelated to the TAC, suggesting it is relying on memorized priors rather than grounding in TAC details.
5. **Medium confidence: more updates help train fit, but alone will overfit without solving generalization.** Evidence: 10 epochs on 83 train rows drove training loss down to ~0.474 overall and final logged losses near 0.01, yet heldout remained poor.
6. **High confidence: selector-signature metadata should stay enabled for QLoRA runs.** Evidence: the broad-30 paired ablation improved semantic similarity by +0.3766 and replication F1 micro by +0.3380 on the same model and rows.
7. **Medium confidence: selector-metadata retraining helps broad generalization modestly, but the current 100-row scale is still too small.** Evidence: broad-30 old-adapter selector-on F1 was ~0.5524, while the selector-metadata retrain reached ~0.5954; exact correctness remained 0/30.
8. **Medium-high confidence: decoding was slightly over-penalizing repetition at 1.15.** Evidence: lowering eval repetition penalty to 1.05 improved broad-30 semantic/edit/F1/bytecode and validity without changing model weights.
9. **High confidence: calls are the first curriculum bottleneck.** Evidence: the calls slice has 43 missing call facts and replication F1 micro 0.5422 under the best decode setting.
10. **Medium confidence: a small calls/state-write curriculum needs negative rows and a strict suite gate to be useful.** Evidence: dense positive-only calls and mixed continuations both regressed at least one required comparison.
11. **Medium confidence: naive continuation from the selector-metadata checkpoint is fragile.** Evidence: both call-only and mixed continuations failed the stronger keep criteria despite low train loss and small local metric movements.
12. **Medium confidence: unsupported extra facts may need explicit negative examples.** Evidence: the rejected continuations either lost recall or regressed transfer; the next cheap path is reducing unsupported calls/invented state writes while preserving the 64-row holdout F1.
13. **Low-medium confidence: negative rows need a better schedule or loss balance before they are useful.** Evidence: 10 extra LR 2e-5 steps improved no-call metrics but regressed no-state metrics and validity.
14. **Medium confidence: unsupported calls are the most robust hallucination target on simple negatives.** Evidence: even after simplifying ABI complexity, simple no-state still has 117 unsupported calls and simple no-call still has 43 unsupported calls.
15. **High confidence: repetition penalty is not the fix for negative hallucinations.** Evidence: 1.15 trades no-call improvement for large no-state regression, and 1.10 regresses both simple controls.
16. **Medium confidence: grounding constraints need to be trained, not only added at inference.** Evidence: an untrained prompt hint changed output distribution but caused correctness regressions and more invented events.
17. **High confidence: pure negative rows are a cleaner hallucination probe than single-category negatives.** Evidence: requiring both no calls and no state writes keeps selector mismatches relatively low while exposing unsupported calls/state writes directly.
18. **High confidence: the call/state hallucination issue survives corrected state-write labels.** Evidence: the rebuilt pure-negative eval still has 74 unsupported calls and 36 invented state writes.
19. **High confidence: behavior-balanced sampling is now a better next path than random scale.** Evidence: the random 300-row run improved some local slices but failed broad/holdout/large gates, while the new fallback curriculum directly covers calls, state writes, guards, returns, events, and pure negatives without overlapping gates.
20. **Medium-high confidence: a full deduplicated/body-balanced run may outperform small random runs.** Evidence: coverage is broad enough across behavior categories, but small random runs likely overfit dominant bodies/boilerplate before learning rare TAC-conditioned semantics.
21. **Medium-high confidence: the full run is under-optimized rather than pipeline-broken.** Evidence: one epoch over 13,635 train rows left train-first-30 at F1 0.6087/bytecode 0.5133 with train loss 0.7058, while prior small overfits can memorize exactly and prompt truncation stayed zero.
22. **High confidence: the first full-run gate gains were not trustworthy heldout gains.** Evidence: the full split overlapped every fixed gate, including 61/64 holdout64 and 154/192 large192 rows in train. The model still underfit many overlapping rows, but those gates can no longer be treated as heldout for that run.
23. **Medium-high confidence: duplicate-heavy raw scale dilutes useful TAC-conditioned signal compared with body balancing.** Evidence: the raw run used all 31,577 rows and 6,431 steps but underperformed the 16,759-row body-balanced run on the built-in heldout test while still failing behavior targets.
24. **High confidence: the raw full-source gate pass is contamination, not clean generalization.** Evidence: all fixed gate body hashes overlapped the raw split, and heavily overlapped slices such as holdout64 achieved very high scores after 63/64 identities landed in train.
25. **High confidence: compiler-version augmentation is lower value than unique-body coverage.** Evidence: the source export had 14,818 duplicate-body rows before body balancing, and the best current full-run path already caps by `metadata.body_hash`; multi-version rows mostly add alternate TAC for the same target body rather than new semantic supervision.

## Invalidated assumptions

1. **Invalidated: "The pipeline cannot learn at all."** The corrected 10-row and 30-row overfits achieved exact reproduction, and iteration-4 train-first-30 got 21/30 exact matches.
2. **Invalidated: "Function signature match was a reassuring metric."** The old metric was a keyword-presence check and gave false positives; strict recomputation dropped the 300-row train and heldout signature rates to 0/17.
3. **Invalidated: "Poor metrics were caused mainly by under-generation from low `eval_max_new_tokens`."** Later evals used 256-512 generation tokens and still showed unrelated snippets.
4. **Invalidated: "Prompt truncation explains the failures."** Prompt diagnostics repeatedly showed zero truncation.
5. **Invalidated: "Heldout failure alone proves train/eval wiring is broken."** Same-row overfit success shows the basic wiring works; the failure has shifted toward scale, data, and generalization.
6. **Invalidated: "Wrong function names are only a model-capacity problem."** Local selector provenance recovered all tested signatures; exposing that provenance fixed many function identity errors without changing weights.
7. **Invalidated: "A selector-metadata retrain immediately solves heldout behavior."** The 100-row retrain improved train memorization and broad structured metrics, but heldout/broad outputs still miss bytecode behavior and remain non-deployable.
8. **Invalidated: "0% Solidity-valid on exact overfit outputs proves syntax collapse."** The exact 30-row overfit outputs are scaffold-valid; the old metric was penalizing missing contract context from solc, not malformed Solidity.
9. **Invalidated: "A call-dense continuation by itself will fix the calls slice."** The first 48-row calls continuation slightly improved semantic/bytecode scores but reduced replication F1 and recall on the same fixed calls slice, and it regressed state-write transfer.
10. **Invalidated: "A small broad-30/calls-slice improvement is enough to keep a continuation."** The mixed continuation passed those small checks but failed a larger 64-row non-overlap mixed holdout, so it was reverted.
11. **Invalidated: "The default full body-balanced gate suite was cleanly heldout."** The full body-balanced split was built from the same source as the fixed gates and included many gate body hashes; the runner now excludes those identities by default.
12. **Invalidated: "Using all source rows through `run_train_qwen_qlora_500.sh` makes fixed gates trustworthy."** The raw all-row split overlapped 100% of the fixed gate identities, so fixed-gate improvements from that run cannot be treated as heldout evidence.
13. **Invalidated: "Three compiler versions per training point are useful default augmentation."** Current evidence favors source-aligned, body-deduplicated coverage; the generator now treats multi-version expansion as removed rather than a default or recommended path.

## Suggestions and next experiments

1. **Run paired train/heldout evals for every future training run.** Success criterion: train strict signature match and replication F1 rise first; heldout should then improve on a comparable split before claiming generalization.
2. **Create data-slice evals by function family and behavior.** Evaluate short getters, setters, transfers, calls, returns, guards, events, and storage writes separately. Success criterion: identify the first capability slice that fails before running larger expensive jobs.
3. **Superseded/rejected: random 300-row staged scale-up.** `models/qwen2_5_coder_7b_qlora_500_loop_iter32_selector_metadata_300/` memorized train rows but failed the gate suite, so do not promote this path to 1k. Success criterion to reopen random scale: a future 300-row random seed must improve broad F1, holdout64 bytecode/validity, pure-negative hallucinations, and large192 validity together.
4. **Build behavior slices now that function identity is partially fixed.** Evaluate short getters/setters, calls, returns, guards, events, and storage writes separately. Success criterion: identify whether calls/storage/guards are first-failing capabilities before another broad run.
5. **Create a failure-bucket curriculum.** Start with simple short getters/setters using selector metadata, then add calls, guards, events, and state writes. Success criterion: heldout strict signature and replication F1 improve within each slice before increasing task diversity.
6. **Improve the deployability/scaffold evaluator for function fragments.** Success criterion: exact-match overfit rows should no longer receive 0% Solidity/deployability solely because contract-level state declarations are absent.
7. **Compare base model vs adapter outputs on selector-signature prompts.** Success criterion: final adapter output should differ materially from base output on train rows and reproduce learned targets; if not, investigate adapter loading or checkpoint selection.
8. **Inspect failure buckets for calls, returns, guards, and state writes.** Success criterion: reduce these buckets and improve replication recall without increasing unsupported extra facts.
9. **Train or evaluate a failure-bucket curriculum before another broad run.** Start with rows dominated by calls/member calls, then state writes, guards, returns, and events. Success criterion: each slice improves replication recall and bytecode semantic score on a heldout slice without increasing unsupported calls/state writes.
10. **Add compiler-version-aware syntax checks for generated fragments.** The remaining 3/30 broad syntax failures are version-related or true syntax issues: missing visibility under solc 0.5.x, `override` under solc 0.5.7, and `payable(...)` syntax under solc 0.5.17. Success criterion: prompts or targets avoid version-incompatible constructs for the target compiler.
11. **Use `data/curriculum_eval/calls_state64_holdout_nonoverlap.jsonl` as a minimum keep gate for future curricula.** Success criterion: improve broad-30, calls-23/state-17, and this 64-row holdout together.
12. **Try data-quality/negative-example changes before more continuation.** Add unsupported-call/state-write negative examples or target-normalization filters; success criterion: unsupported_calls and invented_state_writes fall without reducing recall on the 64-row holdout.
13. **Gate any future continuation with `scripts/eval_gate_suite.py`.** Success criterion: `keep_candidate` across broad-30, calls-23, state-17, and `calls_state64_holdout_nonoverlap`; any regression in F1, bytecode score, semantic similarity, or Solidity validity rejects the model.
14. **Prefer target-holdout early stops for exploratory curricula.** Before running the full four-slice suite, evaluate the slices directly targeted by the curriculum; success criterion: both target holdouts improve or remain flat before spending time on broad/calls/state/holdout64.
15. **Use simple negative controls before training on negative examples.** Success criterion: reduce unsupported calls and invented state writes on `no_calls_simple64_nonoverlap` and `no_state_writes_simple64_nonoverlap` without regressing F1, bytecode score, semantic similarity, or Solidity validity.
16. **Move next effort to data/model conditioning, not decoding.** Success criterion: a prompt or curriculum change improves both simple negative controls and still passes broad/calls/state/holdout64.
17. **If grounding instructions are retried, include them during fine-tuning and gate them against simple negatives first.** Success criterion: unsupported calls/state fall without F1/bytecode/validity regression on both simple negative controls.
18. **Gate call/state hallucination fixes on the pure negative control first.** Success criterion: reduce unsupported calls below 74 and invented state writes below 30 on `no_calls_no_state_simple64_nonoverlap` without lowering F1, bytecode score, semantic similarity, or validity.
19. **Recompute any no-state/pure-negative comparison generated before the nested mapping fix.** Success criterion: use evals produced after `src/replication_metrics.py` handles nested mapping LHS assignments.
20. **Before a 1k+ run, add or designate a larger stratified heldout gate.** Success criterion: use at least 128-256 non-overlap rows covering calls, state writes, guards, events, returns, simple negatives, and pure negatives so scale-up decisions are not made from broad-30/calls-23/state-17 smoke slices alone.
21. **Train/evaluate the fallback balanced300 curriculum before any 1k random run.** Candidate source: `data/curriculum/fallback_balanced300_iter32.jsonl`. Success criterion: broad30 F1 > 0.6049, holdout64 bytecode/validity do not regress, large192 validity >= 0.9219, and pure-negative unsupported calls/state writes do not increase.
22. **Rerun the full body-balanced experiment with gate exclusions.** The first full run was contaminated by gate overlap, and the patched runner now excludes broad/calls/state/holdout64/pure-negative/large192 body identities before body balancing. Success criterion: the eval-clean full run improves true-heldout F1/bytecode/semantic without scaffold-valid regressions and without increasing pure-negative hallucinations.
23. **Checkpoint-3000 is rejected, but it narrows the failure mode.** It preserves validity on broad/calls/state and improves pure-negative64, but holdout64 and large192 still have real scaffold runaways. The 1024-token sweep only extended the same malformed rows to the larger cap. Success criterion to reopen checkpoint-3000: a data or stopping change must eliminate holdout/large malformed cap rows without reducing behavior metrics.
24. **Do not use the raw all-row sampled runner as an acceptance path unless gate exclusions are added.** If `run_train_qwen_qlora_500.sh` is reused for full-source training, success must come from its generated heldout test plus an external clean gate suite, not the existing fixed gates that are embedded in the source. Success criterion: no fixed gate body hashes in train/val/test, or use a separately heldout eval source.
25. **Prefer body-balanced/eval-clean full runs over raw duplicate-heavy full runs.** The next clean full experiment should either resume/rerun `run_train_qwen_qlora_full_body_balanced.sh` with exclusions or add equivalent body-hash capping/exclusion support to the sampled runner. Success criterion: beat raw full test F1 0.6281 and body-balanced test F1 0.6612 while keeping Solidity validity >= 0.9619 and reducing hallucination rate below 35.37%.
26. **When regenerating `data/hf_training_dataset.jsonl`, use source-aligned single-compiler generation.** Success criterion: compile manifests show `compiler_config_policy=single_source_aligned`, compile jobs are about one per prepared contract, and exported duplicate-body pressure drops without reducing behavior coverage.

## Artifact map

- 300-row heldout eval: `results/eval_1782602299.json`
- 300-row first-17-train eval: `results/eval_1782603004.json`
- Corrected 10-row overfit eval: `results/eval_1782604331.json`
- Iteration-4 100-row heldout eval: `results/eval_1782605340.json`
- Iteration-4 first-30-train eval: `results/eval_1782605444.json`
- Iteration-4 model: `models/qwen2_5_coder_7b_qlora_500_loop_iter4_sample100_updates/`
- Iteration-5 30-row overfit eval: `results/eval_1782607225.json`
- Iteration-5 30-row overfit model: `models/qwen2_5_coder_7b_qlora_overfit_sanity_loop_iter5_overfit30_seeded/`
- Selector prompt heldout-6 ablation: `results/eval_1782608360.json`
- Broad-30 selector-off ablation: `results/eval_1782608489.json`
- Broad-30 selector-on ablation: `results/eval_1782608594.json`
- Broad-30 eval slice: `data/loop_iter10_selector_prompt_broad_eval/broad30_excluding_iter4.jsonl`
- Selector-metadata retrain heldout-6 eval: `results/eval_1782609484.json`
- Selector-metadata retrain train-first-30 eval: `results/eval_1782609635.json`
- Selector-metadata retrain broad-30 eval: `results/eval_1782609733.json`
- Context-validity broad-30 diagnostic eval: `results/eval_1782615573.json`
- Final context/segment broad-30 diagnostic eval: `results/eval_1782615778.json`
- Repetition penalty 1.0 eval: `results/eval_1782616279.json`
- Repetition penalty 1.05 eval: `results/eval_1782616380.json`
- Repetition penalty 1.10 eval: `results/eval_1782616492.json`
- Failure slices manifest: `data/eval_failure_slices/broad30_reppenalty_1_05/slice_manifest.json`
- Calls slice eval: `results/eval_1782616677.json`
- State-write slice eval: `results/eval_1782616749.json`
- Calls curriculum source: `data/curriculum/calls48_nonoverlap.jsonl`
- State-write curriculum source: `data/curriculum/state_writes48_nonoverlap.jsonl`
- Rejected calls-curriculum continuation evals: `results/eval_1782617808.json`, `results/eval_1782617880.json`, `results/eval_1782617977.json`
- Mixed holdout eval slice: `data/curriculum_eval/calls_state64_holdout_nonoverlap.jsonl`
- Rejected mixed-continuation evals: `results/eval_1782618265.json`, `results/eval_1782618328.json`, `results/eval_1782618444.json`, `results/eval_1782618963.json`, `results/eval_1782619383.json`
- Paired comparison tool: `scripts/compare_eval_runs.py`
- Suite gate tool: `scripts/eval_gate_suite.py`
- No-call negative curriculum: `data/curriculum_negative/no_calls64_nonoverlap.jsonl`
- No-state-write negative curriculum: `data/curriculum_negative/no_state_writes64_nonoverlap.jsonl`
- Rejected negative-mixed continuation evals: `results/eval_1782621875.json`, `results/eval_1782622164.json`
- Simple no-call negative control: `data/curriculum_negative/no_calls_simple64_nonoverlap.jsonl`
- Simple no-state-write negative control: `data/curriculum_negative/no_state_writes_simple64_nonoverlap.jsonl`
- Pure no-call/no-state negative control: `data/curriculum_negative/no_calls_no_state_simple64_nonoverlap.jsonl`
- Corrected pure no-call/no-state baseline eval: `results/eval_1782623806.json`
- Corrected simple no-call/no-state negative baselines: `results/eval_1782623967.json`, `results/eval_1782624064.json`
- Corrected core gate baselines: broad `results/eval_1782624189.json`, calls `results/eval_1782624247.json`, state `results/eval_1782624305.json`, holdout64 `results/eval_1782624708.json`
- Corrected broad failure slices: `data/eval_failure_slices/broad30_reppenalty_1_05_fixed_extractor/`
- Rejected 300-row staged scale-up: model `models/qwen2_5_coder_7b_qlora_500_loop_iter32_selector_metadata_300/`, sample `data/qwen_qlora_500_loop_iter32_selector_metadata_300/qwen_qlora_300_sample.jsonl`, train eval `results/eval_1782685917.json`, broad/calls/state/holdout/pure/large evals `results/eval_1782686031.json`, `results/eval_1782686107.json`, `results/eval_1782686175.json`, `results/eval_1782686551.json`, `results/eval_1782686644.json`, `results/eval_1782687640.json`
- Large behavior holdout gate: `data/curriculum_eval/large_stratified192_nonoverlap_iter32.jsonl`; accepted baseline eval `results/eval_1782687160.json`
- Fallback balanced curriculum: `data/curriculum/fallback_balanced300_iter32.jsonl`
- Full body-balanced runner: `run_train_qwen_qlora_full_body_balanced.sh`
- Rejected default full body-balanced run: dataset `data/qwen_qlora_full_body_balanced_20260628-162859/body_balanced_dataset.jsonl`, output `models/qwen2_5_coder_7b_qlora_full_body_balanced_20260628-162859/`, built-in test `results/eval_1782703089.json`, train eval `results/eval_1782703171.json`, gate evals `results/eval_1782703268.json`, `results/eval_1782703344.json`, `results/eval_1782703398.json`, `results/eval_1782703785.json`, `results/eval_1782703860.json`, `results/eval_1782704467.json`
- Checkpoint-3000 rejected gate: eval-ready model `models/qwen2_5_coder_7b_qlora_full_body_balanced_20260628-162859/eval_checkpoint_3000/`, gate dir `models/qwen2_5_coder_7b_qlora_full_body_balanced_20260628-162859/eval_gates_checkpoint_3000/`, evals `results/eval_1782705516.json`, `results/eval_1782705611.json`, `results/eval_1782705682.json`, `results/eval_1782705733.json`, `results/eval_1782706119.json`, `results/eval_1782706195.json`, `results/eval_1782706796.json`
- Checkpoint-3000 1024 decode sweep: `models/qwen2_5_coder_7b_qlora_full_body_balanced_20260628-162859/eval_decode_1024_checkpoint_3000/`, evals `results/eval_1782707568.json`, `results/eval_1782707988.json`
- Eval-clean full-run dry run: `data/qwen_qlora_full_body_balanced_eval_clean_dry_run_manifest_20260628-212523/body_balanced_dataset.jsonl`, manifest `data/qwen_qlora_full_body_balanced_eval_clean_dry_run_manifest_20260628-212523/body_balanced_manifest.json`
- Eval-clean full-run stale/incomplete as of the raw full-run investigation: dataset `data/qwen_qlora_full_body_balanced_eval_clean_20260628-214054/body_balanced_dataset.jsonl`, output `models/qwen2_5_coder_7b_qlora_full_body_balanced_eval_clean_20260628-214054/`, log `/tmp/full_body_balanced_eval_clean_20260628-214054.log`; no `final_model` or latest-results artifact observed
- Raw full-source 1-epoch run: dataset `data/qwen_qlora_500_full_raw_all_1epoch_20260702-000545/qwen_qlora_31577_sample.jsonl`, split manifest `data/qwen_qlora_500_full_raw_all_1epoch_20260702-000545/splits/split_manifest.json`, model `models/qwen2_5_coder_7b_qlora_500_full_raw_all_1epoch_20260702-000545/final_model`, built-in eval `results/eval_1783000992.json`, train-first30 `results/eval_1783001111.json`, gate dir `models/qwen2_5_coder_7b_qlora_500_full_raw_all_1epoch_20260702-000545/eval_gates_raw_full_1epoch/`, gate evals `results/eval_1783001205.json`, `results/eval_1783001296.json`, `results/eval_1783001357.json`, `results/eval_1783001730.json`, `results/eval_1783001806.json`, `results/eval_1783002345.json`, launch log `/tmp/qwen_full_raw_all_1epoch_full_raw_all_1epoch_20260702-000545.log`
- Reusable gate runner: `run_eval_gate_suite_for_model.sh`
- Rejected simple-negative decode sweeps: `results/eval_1782622642.json`, `results/eval_1782622742.json`, `results/eval_1782622846.json`, `results/eval_1782622949.json`, `results/eval_1782623055.json`, `results/eval_1782623161.json`
- Rejected prompt grounding hint evals: `results/eval_1782623346.json`, `results/eval_1782623440.json`
- Selector-metadata retrain model: `models/qwen2_5_coder_7b_qlora_500_loop_iter12_selector_metadata_100/`
- Overfit runner: `run_train_qwen_qlora_overfit_sanity_check.sh`
- Standard sampled QLoRA runner: `run_train_qwen_qlora_500.sh`
- Larger-run decision checkpoint: iteration 31 in this file, based on corrected gates `results/eval_1782624189.json`, `results/eval_1782624247.json`, `results/eval_1782624305.json`, `results/eval_1782624708.json` and negative controls `results/eval_1782623806.json`, `results/eval_1782623967.json`, `results/eval_1782624064.json`
- Single-compiler generation policy: `download_hf_contracts.py`, `src/local_compiler.py`, `src/dataset_pipeline.py`, `train.py`, `tests/test_data_generation_export_issues.py`, `tests/test_dataset_quality_issues.py`, `tests/test_docs_consistency.py`
- Strict signature metric implementation: `src/training_pipeline.py`
- Selector prompt metadata implementation: `src/model_setup.py`
- Evaluation metric tests: `tests/test_evaluation_quality_metrics.py`
